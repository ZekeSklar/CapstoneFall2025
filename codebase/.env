# --- PICK ONE CONFIG ---

# A) Authenticated SMTP (e.g., Office 365, Gmail, etc.)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=bcprintingservicespass@gmail.com
EMAIL_HOST_PASSWORD=gqgyofwqxnwailcv
DEFAULT_FROM_EMAIL=Printing Services <bcprintingservicespass@gmail.com>
ISSUE_SUMMARY_RECIPIENT=sklarz@berea.edu
DEBUG=true
SECRET_KEY=dev-insecure-secret
# Local dev hosts; add your DO domain in App Platform env, not here
ALLOWED_HOSTS=127.0.0.1,localhost
# CSRF trusted origins (include scheme). For DO, set in the platform env as well.
CSRF_TRUSTED_ORIGINS=https://*.ondigitalocean.app
# Where to send new ticket notifications (comma-separated)
EMAIL_TO=sklarz@berea.edu


# --- SNMP settings for live device status ---
# Read-only community string shown in TopAccess
SNMP_COMMUNITY=public
# Timeout (seconds) for SNMP requests
SNMP_TIMEOUT=5
# Retries per request (keep low to avoid blocking)
SNMP_RETRIES=1
# Background/UX refresh interval (seconds)
SNMP_POLL_INTERVAL_SECONDS=300


# B) Campus relay (no auth) — comment out the A-section above and use this:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=true
# EMAIL_HOST_USER=bcprintingservicespass@gmail.com
# EMAIL_HOST_PASSWORD=gqgyofwqxnwailcv
# DEFAULT_FROM_EMAIL=Printing Services <bcprintingservicespass@gmail.com>
# EMAIL_TO=ed@berea.edu,sklarz@berea.edu





